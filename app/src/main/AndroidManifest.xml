<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.poc.invoicepay">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".AddItemsActivity"></activity>
        <activity
            android:name=".ContactsActivity"
            android:label="@string/title_activity_contact" />
        <activity
            android:name=".CreateInvoiceActivity"
            android:label="@string/title_activity_create_invoice" />
        <activity
            android:name=".InvoiceOptionsActivity"
            android:label="@string/title_activity_Invoice_options" />
        <activity
            android:name=".TransactionActivity"
            android:label="@string/title_activity_transaction" />
        <activity
            android:name=".PaymentSuccessActivity"
            android:label="@string/title_activity_payment_success" />
        <activity
            android:name=".EnterUPIPinActivity"
            android:label="@string/title_activity_enter_upipin" />
        <activity
            android:name=".ViewInvoiceActivity"
            android:label="@string/title_activity_view_invoice" />
        <activity
            android:name=".CheckPasscodeActivity"
            android:label="@string/title_activity_check_passcode" />
        <activity
            android:name=".CustomizeInvoiceVerifyDetails"
            android:label="@string/title_verify_details" />
        <activity
            android:name=".CustomizeInvoicePaymentMethod"
            android:label="@string/title_select_payment_type" />

        <service
            android:name=".FCMService"
            android:exported="false" />

        <activity
            android:name=".MakePaymentActivity"
            android:label="@string/title_activity_make_payment" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".PayRequestBroadcastReceiver">
            <intent-filter>
                <action android:name="com.payment.request.CUSTOM_INTENT" />
            </intent-filter>
        </receiver>
    </application>

</manifest>